name: FFI Gen Validate
on:
  workflow_call:
    inputs:
      package-name:
        description: The name of the package being tested
        required: true
        type: string
      working-directory:
        description: The working directory relative to the repo root
        required: true
        type: string
      ffigen-configs:
        description: Space-separated list of ffigen config files to validate
        required: true
        type: string
      os:
        description: The runner OS to use (e.g. macos-26, ubuntu-latest)
        required: true
        type: string

permissions:
  contents: read

jobs:
  ffigen-validate:
    runs-on: ${{ inputs.os }}
    timeout-minutes: 30
    steps:
      - name: Cache Pub dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # 5.0.5
        with:
          path: |
            ~/.pub-cache/hosted
            ~/.pub-cache/git
          key: os:${{ inputs.os }};sdk:stable;packages:${{ inputs.working-directory }};ffigen
          restore-keys: |
            os:${{ inputs.os }};sdk:stable;packages:${{ inputs.working-directory }}
            os:${{ inputs.os }};sdk:stable
            os:${{ inputs.os }}

      - name: Git Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 4.0.0

      - name: Git Submodules
        run: git submodule update --init

      - name: Setup Dart
        uses: dart-lang/setup-dart@2bd11704788983a6a210731656d0042e2df8b2c1 # main
        with:
          sdk: stable

      - name: Setup aft
        shell: bash
        run: dart pub global activate -spath packages/aft

      - name: Bootstrap
        id: bootstrap
        timeout-minutes: 20
        run: aft bootstrap --fail-fast --include=${{ inputs.package-name }} --verbose

      - name: Run ffigen and check for changes
        if: "always() && steps.bootstrap.conclusion == 'success'"
        shell: bash
        working-directory: ${{ inputs.working-directory }}
        run: |
          configs="${{ inputs.ffigen-configs }}"
          for config in $configs; do
            echo "Running ffigen with config: $config"
            dart run ffigen --config="$config"
          done
          if git diff --exit-code -- '*.g.dart'; then
            echo "✅ ffigen output is up to date."
          else
            echo "❌ ffigen output is out of date. Please regenerate bindings."
            exit 1
          fi
