# Generated with aft. To update, run: `aft generate workflows`
name: amplify_auth_cognito_android
on:
  push:
    branches:
      - main
      - stable
      - next
      - chore/android-unit
  pull_request:
    paths:
      - 'packages/auth/amplify_auth_cognito_android/**/*.dart'
      - 'packages/auth/amplify_auth_cognito_android/**/*.yaml'
      - 'packages/auth/amplify_auth_cognito_android/lib/**/*'
      - 'packages/auth/amplify_auth_cognito_android/test/**/*'
      - 'packages/auth/amplify_auth_cognito_android/android/**/*'
      - 'packages/auth/amplify_auth_cognito_android/example/android/**/*'
      - '.github/workflows/flutter_android.yaml'
      - '.github/workflows/amplify_auth_cognito_android.yaml'
  schedule:
    - cron: "0 0 * * 0" # Every Sunday at 00:00
defaults:
  run:
    shell: bash
permissions: read-all

jobs:
  android_test:
    uses: ./.github/workflows/flutter_android.yaml
    with:
      working-directory: packages/auth/amplify_auth_cognito_android
