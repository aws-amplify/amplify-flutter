# Generated with aft. To update, run: `aft generate workflows`
name: amplify_api_android
on:
  push:
    branches:
      - main
      - stable
      - next
      - chore/android-unit
  pull_request:
    paths:
      - 'packages/api/amplify_api_android/**/*.dart'
      - 'packages/api/amplify_api_android/**/*.yaml'
      - 'packages/api/amplify_api_android/lib/**/*'
      - 'packages/api/amplify_api_android/test/**/*'
      - 'packages/api/amplify_api_android/android/**/*'
      - 'packages/api/amplify_api_android/example/android/**/*'
      - '.github/workflows/flutter_android.yaml'
      - '.github/workflows/amplify_api_android.yaml'
  schedule:
    - cron: "0 0 * * 0" # Every Sunday at 00:00
defaults:
  run:
    shell: bash
permissions: read-all

jobs:
  android_test:
    uses: ./.github/workflows/flutter_android.yaml
    with:
      working-directory: packages/api/amplify_api_android
