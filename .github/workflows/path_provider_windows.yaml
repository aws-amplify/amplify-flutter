# Generated with aft. To update, run: `aft generate workflows`
name: path_provider_windows
on:
  push:
    branches:
      - main
      - stable
    paths:
      - '.github/workflows/flutter_vm.yaml'
      - '.github/workflows/path_provider_windows.yaml'
      - 'packages/common/amplify_db_common/example/windows/flutter/ephemeral/.plugin_symlinks/path_provider_windows/**/*.dart'
      - 'packages/common/amplify_db_common/example/windows/flutter/ephemeral/.plugin_symlinks/path_provider_windows/**/*.yaml'
      - 'packages/common/amplify_db_common/example/windows/flutter/ephemeral/.plugin_symlinks/path_provider_windows/lib/**/*'
      - 'packages/common/amplify_db_common/example/windows/flutter/ephemeral/.plugin_symlinks/path_provider_windows/test/**/*'
  pull_request:
    paths:
      - '.github/workflows/flutter_vm.yaml'
      - '.github/workflows/path_provider_windows.yaml'
      - 'packages/common/amplify_db_common/example/windows/flutter/ephemeral/.plugin_symlinks/path_provider_windows/**/*.dart'
      - 'packages/common/amplify_db_common/example/windows/flutter/ephemeral/.plugin_symlinks/path_provider_windows/**/*.yaml'
      - 'packages/common/amplify_db_common/example/windows/flutter/ephemeral/.plugin_symlinks/path_provider_windows/lib/**/*'
      - 'packages/common/amplify_db_common/example/windows/flutter/ephemeral/.plugin_symlinks/path_provider_windows/test/**/*'
  schedule:
    - cron: "0 13 * * *" # Everyday at 06:00 PST
  workflow_dispatch:
defaults:
  run:
    shell: bash

# These permissions are needed to interact with GitHub's OIDC Token endpoint.
permissions:
  id-token: write
  contents: read

# Cancels in-progress job when there is another push to same ref.
# https://docs.github.com/en/actions/using-jobs/using-concurrency#example-only-cancel-in-progress-jobs-or-runs-for-the-current-workflow
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: ./.github/workflows/flutter_vm.yaml
    secrets: inherit
    with:
      package-name: path_provider_windows
      working-directory: packages/common/amplify_db_common/example/windows/flutter/ephemeral/.plugin_symlinks/path_provider_windows
      has-goldens: false
