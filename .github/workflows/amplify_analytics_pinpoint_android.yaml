name: amplify_analytics_pinpoint_android
on:
  push:
    branches:
      - main
      - stable
      - next
    paths:
      - 'packages/analytics/amplify_analytics_pinpoint_android/**/*.dart'
  pull_request:
    paths:
      - 'packages/analytics/amplify_analytics_pinpoint_android/**/*.dart'
  schedule:
    - cron: "0 0 * * 0"
defaults:
  run:
    shell: bash
permissions: read-all

jobs:
  test:
    uses: ./.github/workflows/flutter_package.yaml
    with:
      working-directory: packages/analytics/amplify_analytics_pinpoint_android
