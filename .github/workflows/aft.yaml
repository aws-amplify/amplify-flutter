name: aft
on:
  push:
    branches:
      - main
      - stable
      - next
    paths:
      - 'packages/aft/**/*.dart'
  pull_request:
    paths:
      - 'packages/aft/**/*.dart'
  schedule:
    - cron: "0 0 * * 0"
defaults:
  run:
    shell: bash
permissions: read-all

jobs:
  test:
    uses: ./.github/workflows/dart_package.yaml
    with:
      working-directory: packages/aft
