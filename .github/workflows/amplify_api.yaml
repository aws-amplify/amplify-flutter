name: amplify_api
on:
  push:
    branches:
      - main
      - stable
      - next
  pull_request:
  schedule:
    - cron: "0 0 * * 0" # Every Sunday at 00:00
defaults:
  run:
    shell: bash
permissions: read-all

jobs:
  test:
    id: test-vm
    uses: ./.github/workflows/flutter_vm.yaml.yaml
    with:
      working-directory: packages/api/amplify_api

