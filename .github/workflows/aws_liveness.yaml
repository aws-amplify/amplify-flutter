# Generated with aft. To update, run: `aft generate workflows`
name: aws_liveness
on:
  push:
    branches:
      - main
      - stable
    paths:
      - '.github/workflows/aws_liveness.yaml'
      - '.github/workflows/flutter_vm.yaml'
      - 'packages/amplify_lints/lib/**/*.yaml'
      - 'packages/amplify_lints/pubspec.yaml'
      - 'packages/aws_liveness/**/*.dart'
      - 'packages/aws_liveness/**/*.yaml'
      - 'packages/aws_liveness/lib/**/*'
      - 'packages/aws_liveness/test/**/*'
  pull_request:
    paths:
      - '.github/workflows/aws_liveness.yaml'
      - '.github/workflows/flutter_vm.yaml'
      - 'packages/amplify_lints/lib/**/*.yaml'
      - 'packages/amplify_lints/pubspec.yaml'
      - 'packages/aws_liveness/**/*.dart'
      - 'packages/aws_liveness/**/*.yaml'
      - 'packages/aws_liveness/lib/**/*'
      - 'packages/aws_liveness/test/**/*'
  schedule:
    - cron: "0 13 * * 1" # Every Monday at 06:00 PST
  workflow_dispatch:
defaults:
  run:
    shell: bash

# These permissions are needed to interact with GitHub's OIDC Token endpoint.
permissions:
  id-token: write
  contents: read

# Cancels in-progress job when there is another push to same ref.
# https://docs.github.com/en/actions/using-jobs/using-concurrency#example-only-cancel-in-progress-jobs-or-runs-for-the-current-workflow
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: ./.github/workflows/flutter_vm.yaml
    secrets: inherit
    with:
      package-name: aws_liveness
      working-directory: packages/aws_liveness
      has-goldens: false
