# Configuration overrides for Dart beta (3.0).

dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
  test: ^1.22.1

environment:
  sdk: '>=3.0.0-0 <3.0.0'
  flutter: '>=3.8.0-10.1.pre'
