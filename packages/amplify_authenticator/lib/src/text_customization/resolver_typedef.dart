import 'package:flutter/widgets.dart';

/// A basic type for Authenticator string resolvers
///
/// Intended to ensure that every resolver is a function that
/// returns a string
typedef String StringResolver();
