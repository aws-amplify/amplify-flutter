name: amplify_analytics_pinpoint_dart
description: Amplify Analytics Pinpoint for Dart
version: 0.1.0
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  amplify_core: ^0.5.0
  amplify_secure_storage_dart: ^0.1.0
  aws_common: ^0.1.0
  aws_signature_v4: ^0.1.0
  built_collection: ^5.0.0
  built_value: '>=8.2.0 <8.3.0'
  meta: ^1.5.0
  smithy:
    hosted: https://pub.dillonnys.com
    version: ^0.5.0
  smithy_aws:
    hosted: https://pub.dillonnys.com
    version: ^0.5.0
  uuid: ^3.0.0
  sqlite3: ^1.7.1
  path: ^1.8.1 # path manipulation
  drift: ^1.7.0 # local database

  # amplify_flutter: ^0.5.0 # widgetbinding - FLUTTER ONLY
  # USE SECURE STORAGE INSTEAD ... shared_preferences: ^2.0.15 # key/value storage - FLUTTER ONLY
  # path_provider: ^2.0.0 # find storage loc for drift (no web..) - FLUTTER ONLY

  # can we just use localstorage by lesnitsky.dev?
  # solves issue of JSON storage:
  # https://pub.dev/packages/localstorage/example

  intl: ^0.17.0 # for time format

dev_dependencies:
  amplify_lints: ^1.0.0
  drift_dev: ^1.7.0
  build_runner: ^2.1.11
  built_value_generator: '>=8.3.0 <8.4.0'
  test: ^1.16.0



  #sqlcipher_flutter_libs: ^0.5.1 # for encoding sql
  #sqlite3_flutter_libs: ^0.5.0 # FLUTTER ONLY