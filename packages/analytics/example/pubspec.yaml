name: analytics_example
description: Amplify Analytics Pinpoint for Dart example
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  amplify_analytics_pinpoint_dart:
    path: ../pinpoint
  #amplify_auth_cognito_dart:
   # path: ../../auth/cognito
  amplify_common:
    path: ../../common

dev_dependencies:
  amplify_lints: ^1.0.0
  build_runner: ^2.1.4
  build_web_compilers: ^3.2.1
