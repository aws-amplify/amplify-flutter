sdk:
  - stable
  - dev

stages:
  - analyze_and_format:
      - group:
          - format
          - analyze: --fatal-infos .
          # - command: ../../tool/license.sh -check
  - unit_test:
      - group:
          - test:
          - test: -p chrome
