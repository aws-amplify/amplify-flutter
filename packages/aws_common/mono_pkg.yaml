sdk:
  - "2.15.0"
  - "2.19.0"

stages:
  - analyze_and_format:
      - group:
          - format
          - analyze: --fatal-infos .
  - unit_test:
      - group:
          - test:
          - test: -p chrome
