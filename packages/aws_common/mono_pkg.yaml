sdk:
  - '2.17.0'
  - stable
  - dev

stages:
  - analyze_and_format:
      - group:
          - format
          - analyze: --fatal-infos .
  - unit_test:
      - test:
      - command: dart run build_runner test -- -p chrome,firefox
      # TODO(dnys1): Investigate why this is not working but `dart test` is.
      # - command: dart run build_runner test --release -- -p chrome,firefox
      - command: dart test -p chrome,firefox
