name: example
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.5.0
  aws_common: ">=0.4.0 <0.5.0"
  aws_signature_v4: ">=0.6.3 <0.7.0"
  collection: ^1.18.0
  path: ^1.8.0
  web: ^1.1.1

dependency_overrides:
  aws_common:
    path: ../../aws_common

dev_dependencies:
  amplify_lints:
    path: ../../amplify_lints
  build_runner: ^2.4.9
  build_web_compilers: ^4.0.0
