sdk:
  - stable
  - dev

stages:
  - analyze_and_format:
      - group:
          - format
          - analyze: --fatal-infos .
  - unit_test:
    - test:
      os:
        - windows
        - macos
        - linux
    - test: -p chrome,firefox
