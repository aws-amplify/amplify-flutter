name: aws_liveness
description: A Flutter plugin for AWS Face Liveness detection using AWS Amplify UI components. Provides cross-platform face liveness verification for Android and iOS.
version: 0.0.1


environment:
  sdk: ^3.0.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  plugin:
    platforms:
      android:
        package: com.code.aws_liveness
        pluginClass: AwsLivenessPlugin
      ios:
        pluginClass: AwsLivenessPlugin

topics:
  - aws
  - face-liveness
  - biometrics
  - authentication
  - rekognition

# screenshots:
#   - description: 'Face Liveness detection in action'
#     path: screenshots/liveness_demo.png
