enum MY_ENUM {
    # Test re-casing
    value_a
    ValueB
    VALUE_C

    # Test unallowed names
    name
    value
}

type EnumModel @model {
    id: ID!
    enum: MY_ENUM
    requiredEnum: MY_ENUM!
}
