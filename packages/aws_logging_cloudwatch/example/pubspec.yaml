name: aws_logging_cloudwatch_example
description: An example app showcasing the aws_logging_cloudwatch package.
publish_to: none
version: 1.0.0

environment:
  sdk: '^3.0.0'

dependencies:
  aws_logging_cloudwatch:
    path: ../

dev_dependencies:
  amplify_lints: ^2.0.0
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
