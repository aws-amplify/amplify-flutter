sdk:
  - stable
  - dev

stages:
  - analyze_and_format:
      - group:
          - format
          - analyze: --fatal-infos lib
  - build_test:
      - test: --tags=build
        sdk:
          - stable
