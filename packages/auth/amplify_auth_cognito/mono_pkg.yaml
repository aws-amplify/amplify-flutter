sdk:
  - stable
  # TODO: Once Flutter Beta's Dart SDK is >=2.17.0
  # - beta

stages:
  - analyze_and_format:
      - group:
          - format
          - analyze: --fatal-infos lib test
  - unit_test:
      - group:
          - command: flutter doctor -v
          - test: --exclude-tags=build # TODO: Re-enable when CI Flutter is updated
          - test: --platform chrome
