# Selected AWS services for smoke testing, representative
# of the six AWS protocols
apis:
  # API Gateway V1 (restJson1)
  apigateway:

  # Config (awsJson1_1)
  config:

  # EC2 (ec2Query)
  # ec2:

  # DynamoDB (awsJson1_0)
  dynamodb:
  streams.dynamodb:

  # CloudFormation, IAM, STS (awsQuery)
  cloudformation:
  iam:
  sts:

  # S3 (restXml)
  s3:

# TODO(dnys1): Remove when S3 fixes their parameter nullability/optionality
ref: bf750f19766048467c676f6841053f9da6c87bf3