name: example_common
description: Common components and utils for example apps.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  meta: ^1.16.0
  web: ^1.1.1

dev_dependencies:
  amplify_lints: ">=2.0.2 <2.1.0"
  test: ^1.22.1
